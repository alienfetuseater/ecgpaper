import { formFeatureObject, formStateObject } from 'interfaces'

/**
 *
 * each instance of state receives a leadStateObject, or leadObject
 * and for that lead creates an array of formFeatureObjects
 * then returns that array
 *
 */
export default function State(
	formFeatureObject: formFeatureObject,
): formStateObject {
	return {}
}
